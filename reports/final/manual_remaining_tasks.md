# Manual Remaining Tasks

- Supply representative real PCAP captures from the intended deployment or isolated lab for production-like PCAP throughput validation.
- Supply licensed, provenance-documented labeled datasets if real-world ML metrics are required.
- Run training/evaluation/calibration on held-out scenario/session groups after supplying sufficient labeled data.
- Install Docker Desktop or another Docker runtime to execute container build/start validation.
- Execute sustained 30/60-second benchmarks on the final presentation hardware.
- Connect and validate against the actual hardware data diode or passive mirror in an isolated authorized environment.
