# Model Performance

No trusted trained model artifacts are bundled. Statistical detector fallback is active for all six detector families. Precision, recall, F1, AUC, threshold calibration, and held-out test metrics are therefore N/A rather than fabricated.

The reusable training pipeline is available through `scripts/train_model.py` and threat-specific wrappers once licensed labeled data is supplied.
