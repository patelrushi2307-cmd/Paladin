# Recon Detector

`ReconDetector` observes host fan-out, port fan-out, scan velocity, destination entropy, and SYN behavior in source-scoped feature vectors. Horizontal, vertical, and mixed exploration are represented as behavioral evidence. The detector never scans or probes; recon means observed reconnaissance-like behavior only.
