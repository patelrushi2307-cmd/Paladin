# Data

`raw/` contains only small benign normalized examples for contract checks. `processed/` is reserved for later feature datasets, and `scenarios/` is reserved for isolated replay scenarios. No attack traffic or generated alerts are included in Part 1.
