# Scenarios

Scenario manifests describe offline replay inputs and separately maintained ground truth. The detection runtime never treats ground truth as a feature. Empty manifests are preparation only and do not claim detection results.
