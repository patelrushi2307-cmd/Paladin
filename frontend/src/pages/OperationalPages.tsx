import { useEffect, useState } from 'react';
import type { FeatureMetrics, IngestMetrics } from '../types/ingest';
const API_URL = import.meta.env.VITE_API_URL ?? 'http://localhost:8000';
export function PerformancePage() { const [metrics, setMetrics] = useState<IngestMetrics | null>(null); const [features, setFeatures] = useState<FeatureMetrics | null>(null); useEffect(() => { void Promise.all([fetch(`${API_URL}/api/ingest/metrics`).then((r) => r.json()), fetch(`${API_URL}/api/features/status`).then((r) => r.json())]).then(([ingest, feature]) => { setMetrics(ingest); setFeatures(feature); }); }, []); return <main className="route-page"><span className="eyebrow">PERFORMANCE / RUNTIME MEASUREMENTS</span><h2>Pipeline performance</h2><div className="performance-grid"><div><span>TARGET THROUGHPUT</span><strong>500 flows/sec</strong><small>Prototype target</small></div><div><span>MEASURED THROUGHPUT</span><strong>{metrics?.flows_per_second.toFixed(2) ?? 'N/A'} flows/sec</strong><small>Current rolling runtime</small></div><div><span>FEATURE RATE</span><strong>{features?.feature_vectors_per_sec.toFixed(2) ?? 'N/A'}</strong><small>Feature vectors/sec</small></div><div><span>QUEUE UTILIZATION</span><strong>{metrics ? `${metrics.queue_size} / ${metrics.queue_maxsize}` : 'N/A'}</strong><small>Bounded queue</small></div><div><span>FEATURE LATENCY</span><strong>{features?.feature_processing_latency_ms.toFixed(3) ?? 'N/A'} ms</strong><small>Processing, not end-to-end alert latency</small></div><div><span>DROPS / ERRORS</span><strong>{metrics?.events_dropped ?? 'N/A'} / {features?.feature_errors ?? 'N/A'}</strong><small>Runtime counters</small></div></div></main>; }
export function ArchitecturePage() { return <main className="route-page architecture-page"><span className="eyebrow">SYSTEM / PASSIVE ARCHITECTURE</span><h2>Observe. Detect. Explain.</h2><div className="architecture-flow"><strong>ONE-WAY INPUT</strong><b>↓</b><strong>FLOW EVENT</strong><b>↓</b><strong>FEATURE VECTOR</strong><b>↓</b><strong>DETECTION</strong><b>↓</b><strong>SCORE FUSION</strong><b>↓</b><strong>ALERT</strong></div><div className="posture-grid"><span>RECEIVE ONLY <b>ACTIVE</b></span><span>RETURN PATH <b>NONE</b></span><span>ACTIVE PROBING <b>DISABLED</b></span><span>PAYLOAD DECRYPTION <b>OFF</b></span><span>INLINE BLOCKING <b>UNAVAILABLE</b></span></div></main>; }
export function ModelsPage() { const [models, setModels] = useState<Array<{ threat_class: string; model_id: string; algorithm: string; calibration_status: string; loaded: boolean; model_available: boolean }>>([]); useEffect(() => { void fetch(`${API_URL}/api/models/status`).then((r) => r.json()).then(setModels); }, []); return <main className="route-page"><span className="eyebrow">MODEL INTELLIGENCE / RUNTIME STATE</span><h2>Models and fallback</h2><div className="model-grid">{models.length ? models.map((model) => <div key={model.model_id}><strong>{model.threat_class}</strong><span>Model <b>{model.model_id}</b></span><span>Loaded <b>{model.loaded ? 'YES' : 'NO'}</b></span><span>Calibrated <b>{model.calibration_status}</b></span></div>) : <div><strong>Statistical fallback</strong><span>No trusted model artifacts are registered.</span><span>Detector operation remains available.</span></div>}</div></main>; }
